# 前端开发说明

## 当前输入

原型来源：

- `D:/OneDrive/Desktop/云摊坊-用户端.zip`
- `D:/OneDrive/Desktop/云摊坊-商家端.zip`
- `D:/OneDrive/Desktop/云摊坊-管理后台.zip`

识别结论：

- 用户端 PDF 原型约 105 页，主要是移动端 H5。
- 商家端 PDF 原型约 136 页，主要是移动端 H5。
- 管理后台 PDF 原型约 179 页，主要是桌面端后台。
- 移动端原型比例约为 375x812，但实现时必须同时支持手机浏览器和 PC 浏览器。

## 硬性要求

- 不做微信小程序，不新增小程序目录。
- 用户端和商家端都放在 `frontend/apps/h5`，移动端优先，同时适配 PC。
- 管理后台放在 `frontend/apps/admin`，以 PC 使用为主，同时保证窄屏可用。
- 前端页面先使用 mock 数据和 API 封装，不阻塞后端业务表完整实现。
- 页面应优先还原原型中的信息结构、导航层级、主要业务入口和状态表达，不追求一次性补齐所有 400+ 页面。
- 设计、业务顺序、接口约定要写入文档，便于后续 AI 接手。

## 响应式原则

H5 用户端和商家端：

- 手机端：单列信息流、底部 Tab、顶部导航、卡片式业务入口。
- PC 端：居中应用壳，主内容最大宽度控制在 1180px；首页、列表、工作台等页面使用双列或三列网格增强信息密度。
- 页面元素不能依赖固定 375px 宽度；使用 `minmax()`、`clamp()`、`max-width`、`grid`、`flex-wrap`。
- 业务按钮、状态标签、列表项在 PC 端不能被拉伸到难以阅读。

管理后台：

- 桌面端使用侧边栏、顶部栏、面包屑、表格、筛选区。
- 窄屏下侧边栏和内容区不能重叠，后续可演进为抽屉菜单。

## 开发顺序

### 阶段 1：前端基础与核心入口

1. 建立 H5 响应式设计基底：颜色、字号、间距、卡片、状态标签、列表、底部 Tab、PC 应用壳。
2. 用户端核心页面：首页、摊位列表、摊位详情、订单、我的。
3. 商家端核心页面：商家开店、商家工作台、资质管理、摊位/出摊、订单处理、我的。
4. 管理后台基础布局继续完善：登录、数据总览、商家审核、商品管理、订单/投诉列表。

### 阶段 2：用户端主流程

1. 登录/注册。
2. 附近摊位、搜索筛选、特色专区。
3. 摊位详情、商品、评价、导航 mock。
4. 预约下单、订单状态、评价。
5. 收藏、关注、订阅、投诉。

### 阶段 3：商家端主流程

1. 入驻备案表单。
2. 资质材料上传与特殊身份/公益标签。
3. 合法摊位查询、预约、出摊日历、经营打卡。
4. 商品上架、媒资、摊主故事、营销活动。
5. 订单处理、评价回复、经营数据。

### 阶段 4：管理后台主流程

1. RBAC 菜单和权限占位。
2. 商家入驻、资质、特殊身份审核。
3. 区域、摊位、预约审批。
4. 商品内容审核、评价管理。
5. 投诉工单、违规记录、超时告警。
6. 政策公告、通知、数据看板。

### 阶段 5：真实服务接入

1. 地图真实 Web API。
2. 支付、退款、对账。
3. 短信、对象存储、内容安全。
4. 推荐算法和智能订单 Agent。

## 当前实现策略

- 先在 `frontend/apps/h5` 建立用户/商家共用响应式组件风格。
- 路由保持与 `README.md` 和后端 API 骨架一致。
- mock 数据就近放在页面或后续抽到 `packages/shared`，接口调用通过 `packages/api`。
- 每个阶段完成后必须运行 `corepack pnpm -r --filter './packages/**' --filter './apps/**' build`。

## 开发进度

### 2026-06-06 阶段 1A

- 已建立 `frontend/apps/h5` 响应式应用壳。
- 手机端保留底部 Tab；PC 端使用左侧导航和居中内容壳。
- 已完成用户端核心入口：首页、摊位列表、摊位详情、我的订单、个人中心。
- 已完成商家端入口：商家工作台、摊主入驻申请。
- mock 数据位于 `frontend/apps/h5/src/data/mock.ts`。
- 验证命令：`corepack pnpm -r --filter './packages/**' --filter './apps/**' build`。

### 2026-06-06 阶段 1B

- 已补齐商家端核心页面：资质管理、摊位预约与出摊管理、商品管理、订单处理、评价回复、摊主故事。
- 商家端页面均按手机单列、PC 多列布局实现。
- 仍使用 mock 数据，后续再接 `/api/vendor/**` 接口。
- 下一步建议进入阶段 1C：完善管理后台核心列表页，或进入阶段 2 用户端主流程的登录/注册、投诉、评价和订单详情。

### 2026-06-06 阶段 1C

- 已将管理后台通用占位页升级为业务化工作台。
- 新增 `frontend/apps/admin/src/data/mock.ts`，按路由提供后台模块配置、指标、筛选项、表格列和 mock 行数据。
- 已覆盖数据总览、入驻审核、资质审核、特殊身份/公益标签审核、投诉工单、内容审核等核心后台页面的数据结构。
- 后台列表页包含筛选区、指标卡、表格操作、分页和数据总览待办流程。
- 后台样式已补充窄屏适配：小屏下侧边栏转横向滚动菜单，筛选区和指标卡自动换行。
- 下一步建议进入阶段 2：用户端登录/注册、订单详情、发布评价、投诉提交与处理进度页面。

### 2026-06-06 阶段 2A

- 已将用户端登录/注册、订单详情、发布评价、我的投诉、提交投诉从占位页升级为独立页面。
- 新增用户端订单详情状态流转、评价四维评分、图文/视频评价上传占位、投诉举证材料上传占位。
- 扩展 `frontend/apps/h5/src/data/mock.ts` 的用户订单、投诉、评价维度 mock 数据。
- 相关页面继续遵守手机单列、PC 双列/多列响应式要求。
- 下一步建议继续阶段 2B：完善用户端收藏、关注、订阅提醒、订单支付/退款占位，以及摊位详情中的预约下单表单。

### 2026-06-06 阶段 2B

- 已新增预约下单页 `/stalls/:id/reserve`，包含商品、数量、取货时间、联系方式和支付说明占位。
- 已新增收藏/关注页 `/favorites`，覆盖摊位、商品、摊主三类收藏关注关系。
- 已新增出摊提醒页 `/subscriptions`，覆盖站内信和网页通知授权占位。
- 摊位详情已接入预约下单、关注摊主、收藏摊位、订阅提醒入口。
- 订单详情已补充 H5 支付、申请退款、支付记录状态占位。
- 个人中心已补充收藏关注和出摊提醒入口。
- 下一步建议进入阶段 2C：补用户端设置、帮助客服、消息中心、足迹/评价列表，或开始把 H5 mock 数据替换为 `/api` 封装调用。

### 2026-06-06 阶段 2C / 3A / 4B

- 已补齐用户端“我的”二级页骨架：消息中心、帮助与客服、意见反馈、我的足迹、我的评价、我的钱包、设置。
- 已补齐商家端扩展入口骨架：店铺信息、营销活动、分享推广、商家设置、商家帮助中心。
- 已将商家工作台入口扩展到商家“我的”和运营配置类页面。
- 已补齐管理后台所有菜单的业务化 mock 配置：区域、摊位、摊位预约、违规、政策、通知、用户、角色、系统设置均不再只用默认占位。
- 至此，前端主要路由骨架已覆盖用户端、商家端、管理后台的核心与二级入口；后续重点从“页面骨架”转为“接口接入、组件抽取、真实交互和视觉精修”。
- 下一步建议：抽取 H5 通用列表/表单/状态组件到 `packages/ui`，并逐步把 mock 替换为 `packages/api` 调用。

### 2026-06-06 首页响应式重构

- 已回退上一轮过度贴原型的 H5 页面改动，避免 PC 端出现手机原型布局硬套问题。
- 新版用户端首页采用同一份内容结构：Hero、搜索、常用服务、特色专区、分类筛选、附近摊位、平台提醒。
- 移动端使用单列和双列快捷入口；PC 端使用更宽内容壳、Hero 双栏、快捷入口六列、摊位双列加提醒侧栏。
- 首页所有明显入口均接入现有路由，避免“展示了但不能点击”的入口。
- 验证命令：`corepack pnpm --filter @yuntanfang/h5 build`。

### 2026-06-06 普通用户首页收敛

- H5 主导航先按普通用户视角收敛，隐藏“摊主”入口。
- 首页移除摊主入驻、摊主服务、经营概览等普通用户不应直接看到的内容。
- 首页分类筛选改为当前页状态筛选，不再点击后跳转到摊位列表页。
- 验证命令：`corepack pnpm --filter @yuntanfang/h5 build`。

### 2026-06-06 用户首页视觉美化

- 使用 `frontend-design` skill 的视觉方向，首页改为“城市早市票据感”风格。
- Hero 新增附近摊位预览场景，去除经营数据，保留普通用户可见内容。
- 搜索、常用服务、特色专区、摊位卡片、平台提醒补充更强层次、色彩和 hover 动效。
- 分类筛选增加空状态提示，避免无数据分类显示空白。
- 验证命令：`corepack pnpm --filter @yuntanfang/h5 build`。

### 2026-06-06 导航与 Hero 细节优化

- 左侧桌面导航增强交互：活跃指示条、图标底盘、hover 位移、阴影和品牌区质感。
- 首页 Hero 去掉标题下方辅助小字，首屏更干净。
- 验证命令：`corepack pnpm --filter @yuntanfang/h5 build`。

### 2026-06-06 H5 全局页面样式优化

- 使用 `frontend-design` skill，将 H5 其余页面统一到“城市早市票据感”视觉体系。
- 全局优化 `hero-card`、`page-header`、`card`、`list-card`、`metric-card`、`chip`、`status-tag`、表单、上传、时间线和 Vant 搜索/评分/底部导航样式。
- 摊位列表搜索文案从“摊主名称”改为“位置”，个人中心移除普通用户视角下的“摊主入驻”入口。
- 覆盖页面包括摊位列表、摊位详情、订单、订单详情、预约、评价、收藏、投诉、消息/钱包/帮助等通用骨架页面，以及当前保留的商家骨架页。
- 验证命令：`corepack pnpm --filter @yuntanfang/h5 build`。

### 2026-06-06 测试登录与端区分

- 新增前端 mock 登录账号：`test1/123456` 普通用户、`test2/123456` 商家、`test3/123456` 管理后台。
- `@yuntanfang/shared` 新增 mock auth/session 工具，H5 和管理后台共用。
- H5 登录页支持 test1/test2，登录后分别进入普通用户首页和商家工作台；商家路由增加临时守卫。
- 管理后台登录页支持 test3，后台路由增加 admin 临时守卫。
- 数据库 `create_database.sql` 增加账号类型/密码占位字段和测试账号种子，test2 同步生成示例商家记录。
- 验证命令：`corepack pnpm -r --filter './packages/**' --filter './apps/**' build`。

### 2026-06-06 H5 登录入口调整

- H5 未登录时访问任意页面都会跳转到 `/login`，不再默认进入首页。
- `/login` 独立渲染整屏登录页，不显示左侧导航和底部 Tab。
- H5 导航栏移除登录/退出入口，后续退出入口可放到“我的/设置”。
- 已登录后访问 `/login` 会按角色回到普通用户首页或商家工作台。
- 验证命令：`corepack pnpm --filter @yuntanfang/h5 build`。

### 2026-06-06 我的页退出登录

- “我的”页显示当前 mock 登录账号和角色标签。
- “我的”页新增退出登录操作，清除 mock session 后回到 `/login`。
- 验证命令：`corepack pnpm --filter @yuntanfang/h5 build`。

## 交接提示

后续 AI 接手时先读：

1. `docs/frontend-development.md`
2. `docs/module-roadmap.md`
3. `README.md`
4. `frontend/apps/h5/src/router.ts`
5. `frontend/apps/admin/src/router.ts`

如果继续按原型还原页面，先解压原型 zip 到临时目录，不要提交原型 PDF、临时截图或渲染产物。
