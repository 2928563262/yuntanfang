# 云摊坊开发进度记录

## 2026-06-17

### 模块 0：运行基线

- 已启动 Docker Desktop、MySQL、Redis、后端、H5、管理后台。
- 当前访问地址：
  - H5：`http://localhost:5173`
  - 管理后台：`http://localhost:5174`
  - 后端：`http://localhost:8080`
- 已确认 H5、管理后台和后端端口可用。

### 模块 1：待审核商家与摊位释放验收链路

- 新增迁移 `V9__old_town_tofu_products.sql`，给“老城豆花摊”补齐真实商品：
  - 老城手作豆花，8.00 元。
  - 咸口豆腐脑，9.00 元。
  - 桂花冰豆浆，6.00 元。
- 商家端 `/vendor/stall-reservations` 接口改为返回摊位名称、摊位审核状态、营业状态、驳回原因。
- H5 登录页补充 `test4 / 123456` 待审核摊主说明。
- H5 商家“摊位预约”页面展示真实摊位名称和驳回原因。
- 已验证：
  - `test4 / 123456` 可登录商家端。
  - `test4` 能看到“老城豆花摊”的待审批预约。
  - 用户端搜索“老城豆花摊”时，在未审批释放前返回空结果。
  - 用户端首页附近摊位只返回“烟火小摊”“乡野新农人鲜铺”两个已释放摊位。

### 模块 2：用户端真实下单链路

- 后端订单创建逻辑改为以后端数据库商品为准：
  - 校验摊位必须已审核释放。
  - 校验商品必须属于当前摊位。
  - 校验商品必须是 `on_sale`。
  - 校验至少选择一个商品，数量至少为 1。
  - 商品名、单价、总价全部由后端商品表计算，禁止信任前端传入的商品名和价格。
- 已验证：
  - 前端传入伪造商品名和 `0.01` 价格时，后端仍按真实商品“招牌烤串(10串) ¥29.90 x2”创建订单，总额为 `59.80`。
  - 订单详情返回真实订单明细。

### 模块 3/4：商家端状态与管理后台四级审核规则

- 用户端“我的订单”列表已支持点击进入订单详情。
- 商家端商品管理补齐摊位绑定：
  - 上架商品时必须选择已释放摊位。
  - 后端校验摊位必须属于当前商家，且 `audit_status=approved`。
  - 新增商品默认 `on_sale`，可直接在用户端摊位详情展示。
  - 商品列表展示所属摊位。
- 已验证：
  - `test4` 的“老城豆花摊”尚未释放时，调用商家商品上架接口会被拒绝，提示只能给已释放且属于自己的摊位上架商品。
- 管理后台已有四个真实审核队列：
  - 入驻审核。
  - 资质审核。
  - 特殊身份/公益标签审核。
  - 摊位预约审批。
- 后端补强摊位预约审批规则：
  - 必须先通过入驻审核。
  - 必须完成资质审核，不能有待审或驳回资质，且至少有一条已通过资质。
  - 必须完成公益/特殊身份审核，不能有待审或驳回申请，且至少有一条已通过申请。
  - 满足以上条件后，摊位预约通过才会释放到用户端。
- 已验证：
  - 对仍有待审资质的摊主直接审批摊位预约，会被后端拒绝，并提示先完成资质审核。
- 管理后台数据总览已接入真实 `/admin/dashboard/overview`：
  - 商家数、用户数、摊位数、订单数、商品数、投诉数来自后端真实统计。
  - 管理端构建已通过。

## 下一步

- 已完整跑通 `test4 -> test3 四级审核 -> test1 搜索老城豆花摊并下单 -> test4 商家端看单`：
  - 审核前，用户端搜索“老城豆花摊”返回空结果。
  - 管理后台依次通过入驻、两条资质、公益身份、摊位预约后，用户端可搜索到“老城豆花摊”。
  - 用户端可读取“老城豆花摊”商品：老城手作豆花、咸口豆腐脑、桂花冰豆浆等。
  - `test1` 对“老城豆花摊”下单成功，订单写入真实订单和订单明细。
  - `test4` 商家端订单接口可看到该订单和商品明细。
- 管理后台审核列表补充驳回原因/审核意见字段，资质、公益身份、摊位预约列表可展示驳回信息。
- 商家端订单处理补充“接单”按钮，形成接单、拒单、备货中、完成的状态操作。
- 商家端订单状态机已补强：
  - `created` 只能流转到 `accepted` 或 `cancelled`。
  - `accepted` 只能流转到 `preparing` 或 `cancelled`。
  - `preparing` 只能流转到 `completed`。
  - `completed`、`reviewed`、`cancelled` 为终态，不能继续操作。
  - 前端按钮会根据当前状态禁用不可执行操作，并展示后端错误信息。
  - 已验证：待接单订单不能直接完成；接单 -> 备货中 -> 完成可以正常流转。
- 释放后的商家商品上架正向链路已验证：
  - `test4` 在“老城豆花摊”释放后可上架“红糖小豆花”。
  - 用户端摊位商品接口可立即看到新商品。

## 后续待做

- 管理后台审核列表已补充审核详情面板：
  - 支持查看编号、摊主、状态、材料地址、摊位、审核意见、驳回原因等字段。
  - 面板内展示审核顺序提示：入驻审核 → 资质审核 → 特殊身份/公益标签审核 → 摊位预约审批。
  - 可在详情面板直接通过或驳回。
- 商家端商品管理已补充编辑、上下架：
  - 支持编辑商品名、价格、描述、所属摊位。
  - 支持在售/下架状态切换。
  - 后端校验商品必须属于当前商家，摊位必须是已释放且属于当前商家。
- 商家端订单详情页已新增：
  - 路由 `/vendor/orders/:id`。
  - 展示商品明细、用户联系方式、备注、状态操作记录。
  - 支持接单、备货中、完成，并复用后端状态机。
- Agent 已对齐当前真实链路：
  - 更新摊位与商品：非遗手作摊、老城豆花摊、老城手作豆花、咸口豆腐脑、桂花冰豆浆等。
  - 订单草稿状态改为“待接单”。
  - 投诉 action 路由改为 `/complaints/create`。
  - skill 文档同步更新。
- 自动化验收脚本已新增：
  - `deploy/scripts/verify-core-flow.ps1`
  - 覆盖后台四级审核、用户搜索/下单、商家看单、非法状态跳转拒绝、接单到完成状态流转。
  - 已运行通过。
- 用户端订单详情继续补齐状态时间线、评价/投诉入口。
- 用户端订单详情已补投诉入口：
  - 订单详情可直接跳转到提交投诉。
  - 投诉页会携带订单号、商家 ID 和摊位名，自动预填投诉对象与描述前缀。
  - H5 构建已通过。

### 模块 5：投诉工单处理闭环

- 新增迁移 `V10__complaint_review_message_flow.sql`，补齐投诉字段：
  - 关联订单 `order_id`。
  - 投诉类型 `complaint_type`。
  - 处理结果 `process_result`。
  - 分派/处理人、投诉对象、商家名称快照。
- 管理后台 `/admin/complaints` 已接入真实投诉列表。
- 管理后台可处理/关闭投诉，处理结果会回写用户端“我的投诉”。
- 用户端投诉列表展示投诉对象、类型、关联订单、处理结果和更新时间。
- 投诉提交、受理、处理都会生成系统消息。

### 模块 6：评价与商家回复闭环

- 用户完成订单后可提交评分和文字评价。
- 后端校验：
  - 只能评价自己的订单。
  - 订单必须是 `completed`。
  - 同一订单不能重复评价。
- 评价写入订单、用户、商家、摊位关联字段。
- 商家端 `/vendor/reviews` 已接入真实评价列表。
- 商家端可回复评价，回复会展示到用户端摊位详情。
- 商家回复评价后会生成用户消息。

### 模块 7：消息通知链路

- 新增 `t_message` 消息表和 `/api/messages/my`、`/api/messages/{id}/read` 接口。
- 用户端消息中心已接入真实消息。
- 消息支持未读/已读状态。
- 审核结果、订单状态变化、评价回复、投诉提交/处理会生成消息。
- 商家端也可以读取角色消息。

### 模块 8：后端基础权限加固

- 新增 `RoleGuardInterceptor`，对核心路径按 token 角色限制：
  - `/api/admin/**` 仅 admin/auditor/supervisor/system_admin。
  - `/api/vendor/**` 仅 vendor。
  - `/api/orders/**`、`/api/complaints/**`、`/api/reviews/**` 等用户操作仅 consumer。
- 订单详情和投诉详情增加所有权校验。
- 验收脚本已覆盖：
  - 普通用户不能访问商家订单。
  - 商家不能访问普通用户订单。
  - 普通用户不能访问后台投诉工单。

### 模块 9：扩展自动化验收

- `deploy/scripts/verify-core-flow.ps1` 已扩展覆盖：
  - 商品隔离。
  - 创建订单返回商品明细。
  - 用户/商家订单详情商品明细。
  - 订单状态机。
  - 完成订单后评价。
  - 商家查看并回复评价。
  - 摊位详情展示回复。
  - 投诉创建、后台处理、用户回写。
  - 消息生成和已读。
  - 角色权限拒绝。
- 已运行通过。

### MVP 收口：Agent 真实接口、页面真实数据与验收脚本

- Agent 搜索摊位已改为读取真实 `t_stall` / `t_product`：
  - 只返回已审核释放的摊位。
  - 商品列表只返回在售商品。
  - 回复文案和卡片均来自真实查询结果，不再维护静态摊位列表。
- Agent 评价/投诉 action 已收口到真实接口：
  - 评价确认后由前端调用 `POST /api/orders/{id}/reviews`。
  - 投诉确认后由前端调用 `POST /api/complaints`。
  - 不再生成 mock reviewId / complaintId。
  - 去掉“上一单/最近一单”自动映射假订单号的规则，缺具体订单号时追问。
- Agent skill 文档已同步真实 API 规则：
  - `docs/agent-skills/SKILL.md`
  - `skills/search-stalls.md`
  - `skills/submit-review.md`
  - `skills/submit-complaint.md`
- 用户端个人中心统计已改为真实接口：
  - 收藏、订单、评价、投诉、未读消息均从后端读取。
  - 接口失败时显示 `-`，不展示假数字。
- 管理后台首页“待办流程”已改为真实 overview 数据，不再显示固定 mock 数字。
- 自动化验收脚本已补充：
  - Agent 搜索摊位返回真实后端数据。
  - Agent 投诉 payload 可调用真实投诉接口落库。
  - 商品下架后普通用户不能再下单该商品，随后恢复上架。
- 本轮验证：
  - 后端通过 Docker Maven 编译：`mvn -q -DskipTests compile`。
  - H5 和管理后台 TypeScript + Vite 构建通过。
  - 前端 lint：0 error，仅保留 Vue 格式 warning。
  - `deploy/scripts/verify-core-flow.ps1` 已运行通过。
  - H5/Admin 关键路由加载 smoke 已通过：登录、快速开始、摊位、订单、个人中心、商家工作台、管理后台首页、审核与投诉。
